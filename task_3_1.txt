1) В таблице users 

- я бы проставил NOT NULL в определение каждого столбца, т.к. это основные данные пользователя без которых регистрация невозможна.

2) В таблице profiles


- birthday DATE я бы также определил как NOT NULL, 
сделав этот атрибут пользователя обязательным для заполнения. Т.к. в соцсети в теории может быть определенные возрастные ограничения на просмотр определенного контента


3) таблица messages:

- статус сообщения (отправлено, получено, прочтено)
- можно было бы добавить индексацию по отправителю, статусу и дате создания (чтобы база быстрее отрабатывала пользовательские запросы на фильтрацию сообщений)

4) таблица communities:

- id сделать Primary Key, т.к. на него есть прямые ссылки из других таблиц и уникальных пабликов может быть много

5) media_types:

- список видов медиа объектов можно ограничить ENUM (фото, видео, музыка, посты)

6) photo_albums:

- не очень понимаю зачем по дефолту присваивать NULL name и user_id. Ведь при добавлении записи в таблицу альбом создается кем-то (user_id) и ему присваивается какое-то имя(name), т.е. эти значения не могут быть пустыми при наличии id

7) Добавлены след таблицы/связи:

+: родной город/страна, страна/город проживания
+: образование
+: семейный статус
+: музыка и плейлисты

